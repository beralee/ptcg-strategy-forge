# Strategy Forge Marnie demo

This data-only strategy demonstrates the documented PtcgDAP author workflow with exact Godot local card UIDs. The `forge.morgrem.evolve` macro is covered by positive, negative, reordered, mandatory, terminal, hard-tier, veto, unknown-UID, and hidden-field scenarios.

It is Windows-only, `cabt_exportable=false`, test-fixture signed, `execution_trusted=false`, and grants no player, engine, or production authority.
