# 跨仓库架构升级进展（2026-09-09）

**后续更新（2026-09-15 启动）：** 私有控制面已获授权并完成本地账号/公钥/签名提交/
精确对账闭环；两个原失败公网录像任务恢复成功。Forge 最终 155/155 通过。
新接口于 2026-09-16 完成部署，原生 BC 和引擎评估仍未关闭。以下保留 9 月 9 日历史状态，
当前用法与证据见 [控制面 CLI](23-CONTROL-PLANE-CLI.md)。

本次在获准修改 PtcgDAP 与 ptcgdojopage 后继续推进。完整架构升级尚未验收。
当时，控制面位于独立私有仓库，后端修改仍等待单独授权。

## 已实现

- Forge 最近对局列表按可下载系列去重，避免双座位比赛重复占用下载预算。
  新测试先以重复列表失败，再通过；CLI 与录像采集相关 12 项测试通过。
- 网页资格状态按精确枚举判断；提交回执先持久化再请求，服务端接受与后续刷新
  分开记录，不确定响应通过同账号、同 API、同归档哈希对账，避免自动重复提交。
  最终静态构建与 34 项测试通过，本地浏览器 fixture 上传显示已接收。
- PtcgDAP 新录像增加模型输入诊断证据，直接调用运行时投影器和候选域计算器，
  记录 source hash、张量、presence、语义行及当前索引映射。3 项 Godot 测试通过，
  覆盖重排和负向门。旧录像训练资格保持拒绝，尚无新整局训练验收。

## 实际公网联调

`https://api.ptcg.skillserver.cn` 的公开 ladder snapshot 请求成功。最近 60 个
比赛记录存在双座位系列重复，已修复去重。固定两个系列的真实录像下载使用并发 2、
每秒 2 请求、2 系列上限与 32 MiB 总预算；两个系列均在 3 次尝试后返回 HTTP 503，
下载字节数为零。失败状态与可恢复任务已保存；没有把错误响应当作录像或 BC 数据。

采集 ID：`bb8d2ed20fc0502b4246b9f44da08d6516c774c14a35fe5e8df28f284d5389e5`。
任务 ID：`92f1e59fe8942bc280a86ad41a2231e01e2908712989271a9d43449f61e90fe3`。

## 尚未验收

1. 私有后端登录、正式提交及精确对账协议；未登录生产账号、上传策略或部署。
2. 服务端录像 503 修复后的真实下载和恢复验收。
3. 新原生整局录像、跨语言模型投影向量、BC 标签门与真实引擎评估闭环。
   当前新增 Godot 测试仍打印既有 NUL 解析警告，未声明语义哈希跨语言一致。
4. 网页 Blob 回执下载：本地按钮与接收状态已见证，自动化未捕获下载事件，
   尚不能声明文件落盘通过。
5. 扩展引擎录像回归实际运行 5 项：3 项通过，2 项旧精确策略包绑定失败，
   错误 `package_integrity_invalid`。未修改生产/开发准入门来绕过失败。

## 证据与回滚

本地日志位于 `work/upgrade-validation/`：`dojo-final-build-20260909.log`、
`dojo-final-suite-20260909.log`、`dojo-live-sync-20260909.json`、
`dojo-forge-targeted-20260909.log`。外部修改前的原始文件已保留在同目录的
`dojo-backup-20260909` 与 `engine-backup-20260909`，回滚须保留原有用户改动。

本次最终源码全量 Python 回归为 **150/150 通过，488.398 秒**；规则工作区
双构建、Host 与场景验收通过。新 wheel 在源码目录外安装检查通过，规则路径
无需模型依赖，412 个 vendored SDK 文件校验通过。

完整机器可读回执为
[`evidence/cross-repository-upgrade-20260909.json`](../evidence/cross-repository-upgrade-20260909.json)，
包含本次源码、日志、wheel 与回滚备份哈希。它明确标记完整架构仍未完成。
没有提交、推送、发布或安装生产策略。
