# Marnie 18.0 Windows local-deck candidate

Deterministic built-in export fixture using exact Godot local card UIDs. It is Windows-only, cabt_exportable=false, test_fixture_only, execution_trusted=false, and grants no player or match authority.
