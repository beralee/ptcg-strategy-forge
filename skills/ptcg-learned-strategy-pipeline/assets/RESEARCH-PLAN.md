# 学习型策略研发计划

本文件复制到研究工作区 `research/PIPELINE-PLAN.md` 后填写。它是研发记录，不是可直接提交给 Forge 的 JSON/schema。`未绑定`、`未验证`、`pending` 都表示尚未通过；不要把计划数值写成实测。

## 任务范围

- 用户目标及已授权阶段：未绑定
- Forge 根目录、当前 commit 与实际工作树摘要：未绑定
- 可修改仓库/目录，外部源权限：未绑定
- 牌组及精确 60 卡身份：未绑定
- 目标平台、时间/计算预算、终止条件：未绑定
- 当前阶段、最近有效回执、下一项工作：P0 / 未绑定 / 冻结基线

## 绑定身份

| 对象 | 路径/版本 | SHA-256 或证据 |
|---|---|---|
| 规则基线包与完整 fallback | 未绑定 | 未验证 |
| deck manifest / printing / card catalog | 未绑定 | 未验证 |
| Forge 源码 / SDK snapshot | 未绑定 | 未验证 |
| Godot / Host / native ORT / runtime snapshot | 未绑定 | 未验证 |
| tensor / operator / resource profile | 未绑定 | 未验证 |
| 对手 roster / 固定权重 | 未绑定 | 未验证 |
| 原生 trace / 适配器 / 教师 | 未绑定 | 未验证 |
| dataset / split / 确认种子 | 未绑定 | 未验证 |
| 训练代码 / 配置 / checkpoint / exporter | 未绑定 | 未验证 |
| 候选 Actor / 包 | 未绑定 | 未验证 |
| 回滚包及兼容运行时 | 未绑定 | 未验证 |

## 阶段账本

| 阶段 | 状态 | 证据路径与摘要 | 已知缺口/失效原因 |
|---|---|---|---|
| P0 基线和候选域覆盖 | pending | 无 | 待核对 |
| P1 公开投影和双端集成 | pending | 无 | 待核对 |
| P2 真实数据资格与分组切分 | pending | 无 | 待核对 |
| P3 BC/导出/规则 fallback | pending | 无 | 待核对 |
| P4 模型实战与纠错 | pending | 无 | 待核对 |
| P5 群体 PPO | not_requested | 无 | 仅在 RL 纳入任务后启动 |
| P6 冻结包独立对照与交付 | pending | 无 | 待核对 |

状态使用 `pending / in_progress / passed / failed / not_requested`。`passed` 必须绑定实际证据；依赖变化后恢复 pending 并保留历史。

任务明确包含整局 RL 时将 P5 改为 `pending`，其余前置门仍须通过。P0 若缺少目标候选域，记为未知并保持 pending；先完成独立基线审计和所需 P1/P2 接口，再回填，不能把未知记成零。

## 特征与数据契约

- 每个特征的来源/可见性、范围、presence、类别词表与双端对照：未绑定
- 目标 context、模型启用 gate、数量范围和保留规则的窗口：未绑定
- 总窗口/可学习窗口/排除原因，教师与目标 frontier 的关系：未验证
- 决策前观察→候选→选择→接受→引擎见证→张量标签的闭合证据：未验证
- 数据分组、去重、切分、增广及已消费测试集记录：未绑定
- 非法/dirty/缺失轨迹的处理和保留位置：未绑定
- 分 context / 动作 / 对手覆盖、教师执行与同窗查询的来源比例：未验证
- 相同张量不同动作、可互换副本、标签冲突的审核与处理：未验证
- 改选账本：劣选 / 有益 / 等价或可交换 / 信息不足及公开依据：未绑定

## 预先固定的实验

- 本轮唯一假设与变更归因：未绑定
- 对照：规则 / 相同规则＋BC / 相同规则＋BC→RL（按任务取用）
- 对手版本、权重、种子组、双座位、候选与基线局数：未绑定
- 主要指标、关键对局退步限制、统计方法、晋级与停止规则：未绑定
- 数据试运行、开发对照、封存确认集的预算：未绑定
- 数据、特征、目标或架构的消融对照及选模规则：未绑定
- 分情境一致率、语义一致率、损失/校准及关键战术指标：未绑定
- 模型调用/改选/规则分流/异常 fallback/拒绝的审计：未验证
- 本机资源预检、重型任务串行锁、监督器版本/摘要：未绑定
- 输出/分页/系统/TEMP 盘余量，RAM/commit，进程内存和采样路径：未验证
- 单次局数/epochs、时长、输出量上限、停止条件及恢复 checkpoint：未绑定
- 故障注入、短试运行、原子结果与写入失败验收：未验证

## 每轮记录

| run id | 输入/变更摘要 | 产物与证据 | 结果及归因 | 接受/拒绝/下一步 |
|---|---|---|---|---|

不要用后来的报告覆盖失败实验。最终确认集一旦用于调整即不再是独立确认集。

## 交付声明

- 包/模型接口：未验证
- 公开窗口场景与重排：未验证
- 指定 Godot 下真实模型执行：未验证
- 同条件/独立种子强度：未验证
- CABT 规则差分：未声明
- 设备/线上资格/production：未声明
- 未完成范围、回滚身份和可复核下一步：未绑定
